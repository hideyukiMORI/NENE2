# 操作指南：多货币钱包

每用户货币余额，支持存款/取款/转账操作。
字段试验：FT198（`../NENE2-FT/walletlog/`）。包含 VULN-A~L 安全审计。

## 关键模式
- 余额以最小单位（美分）存储——避免浮点精度问题
- 在任何 DB 操作之前拒绝自我转账（422）
- 在 UPDATE 之前检查余额不足（409）
- IDOR：每条钱包和交易查询都带 `WHERE user_id = :uid`
- 货币通过允许列表校验（非用户提供）

## VULN-A~L：全部通过
