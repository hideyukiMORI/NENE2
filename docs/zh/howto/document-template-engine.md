# 操作指南：文档模板引擎

演示带 `{{variable}}` 变量替换功能的模板 CRUD，以及基于管理员密钥的写入权限控制。
字段试验：FT197（`../NENE2-FT/templatelog/`）。

## 模式概述
- 模板上的 `UNIQUE(name)` 约束 → 重复时返回 409
- 列表端点排除 `body` 字段以减少响应体积
- `POST /templates/{id}/render` 接受 `vars` 对象，替换 `{{key}}` 占位符
- 未知变量保持原样（不报错）
- 管理员密钥控制创建/更新/删除；渲染接口对外公开
