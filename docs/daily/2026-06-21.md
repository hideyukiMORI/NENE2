# 日報 — 2026-06-21

## 作業時間帯

2026-06-21。リポジトリ全体の状態把握（マルチエージェントによる横断サーベイ）から始め、検出したドリフトを 1 件ずつ Issue ドリブンで解消した一日。新機能ではなく「サーベイ → 根本まで実測確認 → 修正 → 再発防止・記録」の整備中心。サーベイの指摘を鵜呑みにせず実測で再検証し、誇張・誤検知を切り分けてから手を入れた点が今日の軸。

---

## 本日の成果

### 1. リポジトリ全体サーベイ（状態把握）

10 領域の並列リーダー + ドリフト監査をワークフローで実行し、全体像を把握。バージョン 4 系統一致（`FrameworkInfo` / `openapi.yaml` / `CHANGELOG` / `current.md` = 1.5.329）、main クリーン、open PR/Issue 0、デフォルトテスト 456 件全通過などの健全性を確認しつつ、**ポート / ft-registry / i18n / roadmap** のドリフト候補を抽出した。

- **誤検知の訂正**: サーベイ結果を実測再検証し 2 件の誤りを訂正。
  - 「DX シナリオは 27 件で current.md の 50 件は誇大」→ 実ファイルは **50 件**で記載は正確。
  - 「FT352 のバージョンが 1.5.326 と 1.5.329 で矛盾」→ git log で 1.5.327〜329 が非 FT の通常リリースと確認、**非矛盾**。

### 2. 開発用ポート参照の統一（#1397 / #1398）

#1393 でドキュメントのローカルポートを 8080→8200 に統一した際、設定ファイル側の追従漏れを修正。

| ファイル | 変更 |
|------|------|
| `frontend/vite.config.ts` | dev proxy `/api` ターゲット 8080→8200（`npm run dev` の API が届く実害修正） |
| `frontend/README.md` | 上記 proxy 先（Docker backend）の説明を追従 |
| `smithery.yaml` | 起動手順コメント + `host.docker.internal` 例（2 箇所） |

- **`compose.override.yaml`（8085/3307）は据え置き**: サーベイの「git 追跡済みドリフト」は誤報で、実際は `.gitignore:1` のローカル専用ファイル。書き換えても PR に入らず、各自のローカルポート選択を無断変更するだけなので触らない。
- 残存 8080（tutorial の `php -S` / consumer 手順 / 本番 nginx 例 / SSRF 攻撃標的例）は #1393 で意図的に保持されたもので据え置き。

### 3. ft-registry の実態再同期（#1399 / #1400）

`docs/ft-registry.md` が FT177 で更新停止していた問題を解消。各 FT ディレクトリの `composer.json` の `"FT<番号>: <テーマ>"` を正本として照合。

- サーベイの「FT178〜352 で 175 件欠落」は**誇張**。実際にディレクトリを作成した最後の FT は **FT221**、うち 17 件は既存名の再利用（新規行不要）→ 真の追加対象は **22 件（FT178〜FT214）**。
- 22 件を表に追記し、`new-ft.sh` の台帳 grep が全 22 名に効くことを実測確認（未検出 0）。
- 「FT215 以降（howto-only 運用）」注記を追加し、FT215-352 を捏造で埋めないようにした。

### 4. FT ディレクトリ再利用の調査（PR なし）

ft-registry 監査で見つけた「一部ディレクトリの composer.json FT 番号が台帳と矛盾（例 `taglog`=FT209 / 台帳 FT125）」を調査。

- 17 件すべて 2026-05-26〜27 の約 5 時間に均一形（`src:1 tests:0`）で作成された**バッチ処理の痕跡**で、`../NENE2-FT/` は git 管理外の使い捨てスクラッチ。
- 元 FT・新 FT の双方に別個の howto が存在（`tagging-system.md` + `tag-label-api.md` 等）、正準実装は別リポ **NENE2-examples**（GitHub、130+ apps）に存在。
- 結論: **意図的・無害な再利用**でデータ損失なし → 対応不要。

### 5. i18n 未訳 4 本の翻訳（#1401 / #1402）

英語 260 本に対し全 5 ロケールが 256 本で 4 本未訳だった乖離を解消。`bulk-reorder-api` / `csv-export-formula-injection` / `live-penetration-testing` / `use-window-functions` を翻訳。

- ロケール別に**並列翻訳エージェント 5 体**を起動（各々が同一言語の既存翻訳で用語・体裁を確認）。**4 本 × 5 ロケール = 20 ファイル**。
- 規約準拠: frontmatter なし・H1/散文/見出し/コード内コメントは翻訳・コード/識別子/FT 番号/パスは英語のまま・行末ハードブレーク保持。
- 構造忠実度を実測検証: 全 5 ロケールで行数一致（en −frontmatter）、コードフェンス数が en と完全一致（8/10/20/16）。`composer howto:index` で索引再生成し**全 6 ロケール 260 本に同期**。

### 6. roadmap の鮮度修正（#1403 / #1404）

`docs/roadmap.md` Phase 75（未完了の v2.0 設計フェーズ）の friction スコープ `FT96–FT349` を実態 `FT96–FT352` に更新。

- **Phase 73 の「256 howto guides」は据え置き**: 完了フェーズの達成時点スナップショット（#1302 が当時 256 件で実施）。Phase 71/72 の慣習・#1393 の扱いに倣い、260 へ書き換えると過去 PR の実績を誤記するため変更しない。

### 7. CHANGELOG `[Unreleased]` の判断（PR なし）

1.5.329 リリース以降のコミットを分類。`build(deps)` 14 件はすべて dev/frontend 依存、残りは docs/test/ci/config で、**publish される `src/` ランタイムに影響する notable な変更はゼロ**。Keep a Changelog 的にもリポジトリ慣習（依存 bump は CHANGELOG に載せない）的にも**空が正しい**と確認し、対応不要とした。

### 8. MySQL 統合テストに skip ガードを追加（#1405 / #1406）

DatabaseMySQL スイートが MySQL 未起動時に 20 回リトライ後 `self::fail()` し、setUp 毎の再試行で計 13 回 × 5 秒ハングしてから失敗していた（CLAUDE.md の「自動 skip」と乖離）。

- 共有 trait `Nene2\Tests\Database\MySql\RequiresMySql` を新設し、3 ファイルに完全重複していた `connection()/config()/env()`（各 ~36 行）を集約。
- 到達不能時は `self::markTestSkipped()`、判定を `private static` でキャッシュして初回失敗後の setUp は即スキップ。
- `compose.yaml` が `DB_HOST` を常設するため env 有無では判定できず、「接続試行 → 到達不能なら skip」を採用。
- 実測: MySQL 無しで **13 件全 skip・exit 0**（従来は約 65 秒ハング後に FAIL）、デフォルト test 456 件全通過。**重複 123 行削除 + trait 73 行（正味 −44 行）**。

---

## 数値

- マージ PR: **5 件**（#1398 / #1400 / #1402 / #1404 / #1406）
- 新規 Issue: 5 件（#1397 / #1399 / #1401 / #1403 / #1405、いずれもクローズ済み）
- 調査のみ（PR なし・対応不要と確定）: 2 件（FT ディレクトリ再利用 / CHANGELOG）
- ft-registry: **+22 エントリ**（FT178〜FT214）
- i18n: **全 6 ロケール 260 本に同期**（256 → 260、+20 ファイル）
- MySQL テスト: 約 65 秒 hang+FAIL → graceful skip、テストコード **−44 行**（重複解消）
- VERSION: **1.5.329 据え置き**（本日の全変更が docs/test/config で consumer 影響ゼロ）
- 本体テスト: **456 tests / 1092 assertions**（PHPStan level 8 クリーン継続）

---

## 残課題（次回以降）

| 優先 | 内容 |
|------|------|
| P1 | roadmap に FT171〜FT352 のフェーズ記録がない（鮮度ではなくロードマップ拡張）。FT ループの実績を Phase として追記する余地 |
| P2 | NENE2-examples が howto に ~150 本遅れ（正準実装の量産負債、前回から継続） |
| P3 | リリース粒度のインフレ（前回 P2 継続）。本日は VERSION を据え置けたが、docs/core release の分離方針は未決 |

---

## 所感

「状態を把握して」から始まった一日が、サーベイ → 個別ドリフト解消の連鎖になった。良かったのはサーベイの指摘を一度も鵜呑みにしなかったこと。「175 件欠落」は実 22 件、「DX シナリオ 27 件」は実 50 件、「compose.override がドリフト」は実は gitignore 済みローカルファイル、「FT 番号矛盾」は無害なスクラッチ再利用 — どれも実測で裏を取って初めて正体が分かり、機械的に直していたら捏造エントリや他人のローカル設定破壊をやらかすところだった。i18n は 5 並列翻訳で 20 ファイルを一気に片付けつつ、行数・コードフェンス数の一致で構造忠実度を定量確認できた。MySQL skip ガードは「fail を skip に変える」だけでなく、三重複製を trait に畳んで static キャッシュで高速化まで持っていけた。VERSION を 1.5.329 のまま据え置けた（全変更が consumer 非影響）のも、リリース粒度を無駄に進めない判断として収まりが良い。終わってみればオープン Issue 0 / PR 0 / main クリーン。区切りは上々。
